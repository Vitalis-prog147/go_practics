# Telegram Bot - Система подсчета очков

Telegram бот для подсчета очков команды с автоматическим сбросом каждый день.

## Возможности

- **Таблица очков** - отслеживание очков игроков с красивым форматированием
- **Эмодзи для мест** - визуальное отображение позиций (🥇🥈🥉)
- **Автоматический сброс** - очки сбрасываются каждый день
- **Список команд** - удобная справка по всем командам
- **Валидация команд** - обработка неизвестных команд

## Быстрый старт

### Требования

- Go 1.24.5 или выше
- Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))

### Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd telegram_bot_todo
```

2. Установите зависимости:
```bash
go mod download
```

3. Создайте файл `config.yaml` в корне проекта (см. раздел "Конфигурация" для подробностей):
```
TELEGRAM_BOT_TOKEN=your_bot_token_here
BOT_DEBUG=false
```

4. Запустите бота:
```bash
go run main.go
```

## Команды бота

| Команда | Описание |
|---------|----------|
| `/start` | Начать работу с ботом и показать список команд |
| `/add [имя]` | Добавить очко игроку |
| `/score` | Показать таблицу очков |
| `/help` | Показать справку по командам |

### Примеры использования

```
/add Иван
/add Мария Петрова
/score
/help
```

## Архитектура проекта

Проект организован по принципам чистой архитектуры с разделением ответственности:

```
telegram_bot_todo/
├── bot/              # Логика бота и оркестрация
├── commands/         # Парсинг и типы команд
├── exec/             # Выполнение команд (handlers)
├── models/            # Доменные модели (Scoreboard)
├── format/            # Форматирование вывода
├── config/            # Конфигурация приложения
└── main.go           # Точка входа
```

### Описание пакетов

- **`bot/`** - Инициализация и запуск Telegram бота, обработка обновлений
- **`commands/`** - Определение типов команд, парсинг входящих сообщений
- **`exec/`** - Обработчики команд (start, add, score, help, unknown)
- **`models/`** - Доменная модель Scoreboard с бизнес-логикой
- **`format/`** - Утилиты форматирования (эмодзи для мест)
- **`config/`** - Загрузка конфигурации из переменных окружения

## Конфигурация

### Переменные окружения

| Переменная | Описание | Обязательная |
|------------|----------|--------------|
| `TELEGRAM_BOT_TOKEN` | Токен Telegram бота | Да |
| `BOT_DEBUG` | Режим отладки (true/false) | Нет |

### Файл `config.yaml`

Создайте файл `config.yaml` в корне проекта со следующим содержимым:

```
TELEGRAM_BOT_TOKEN=your_actual_bot_token_here
BOT_DEBUG=false
```

**Описание параметров:**
- `TELEGRAM_BOT_TOKEN` - токен вашего Telegram бота. Получить можно у [@BotFather](https://t.me/BotFather) в Telegram. Формат токена: `число:буквенно-цифровая_строка` (паттерн: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`, где первая часть - это ID бота, вторая - секретный ключ)
- `BOT_DEBUG` - режим отладки. Возможные значения: `true` или `false`. По умолчанию: `false`

**Важно:** 
- Файл `config.yaml` добавлен в `.gitignore` для безопасности
- Не коммитьте реальный токен в репозиторий
- Замените `your_actual_bot_token_here` на реальный токен, полученный от BotFather

## Особенности реализации

### Автоматический сброс очков

Очки автоматически сбрасываются каждый день при:
- Добавлении нового очка (`/add`)
- Просмотре таблицы (`/score`)

### Форматирование таблицы

Таблица очков отображается с:
- 🥇 Эмодзи для первых трех мест
- 🏅 Разнообразные эмодзи для остальных мест
- Сортировкой по убыванию очков
- Алфавитной сортировкой при равных очках

### Обработка ошибок

- Валидация команд с понятными сообщениями об ошибках
- Обработка неизвестных команд с подсказками
- Игнорирование обычных сообщений (не команд)

## Разработка

### Запуск в режиме разработки

```bash
go mod download

go run main.go

go build -o bot main.go
```

### Структура кода

Код следует принципам:
- **Single Responsibility** - каждый пакет отвечает за свою область
- **Dependency Injection** - зависимости передаются через параметры
- **Clean Code** - понятные имена, отсутствие избыточных комментариев
- **Separation of Concerns** - разделение бизнес-логики и инфраструктуры

## Лицензия

Этот проект создан в образовательных целях.

## Автор

Создано для изучения Go и архитектуры приложений.

## Благодарности

- [go-telegram-bot-api](https://github.com/go-telegram-bot-api/telegram-bot-api) - библиотека для работы с Telegram Bot API
- [godotenv](https://github.com/joho/godotenv) - загрузка переменных окружения
